export const connection: connection = {
    CONNECTION_STRING: '1233/abc',
    DB: 'MYSQL',
    DBNAME: 'TEST'
};

export type connection = {
    CONNECTION_STRING: string;
    DB: string,
    DBNAME: string
}